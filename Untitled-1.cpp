#include <iostream>
using namespace std;

int main() {
    int x;
    x = 0;
    while (/**/) {
        //라인 안에서만 해결
    if (x<1) {
//라인 안에서만 해결
        }
//라인 안에서만 해결
    if (/**/) {
//라인 안에서만 해결
//라인 안에서만 해결
    }
    if (x==1) {
        //라인 안에서만 해결
    }
    if (/**/) {
        //라인 안에서만 해결
    }
    cout << "\n";
    //라인 안에서만 해결
}
}
